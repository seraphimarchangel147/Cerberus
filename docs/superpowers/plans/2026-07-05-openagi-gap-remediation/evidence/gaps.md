[
 {
  "id": "G1",
  "area": "ambient-capture / core loop",
  "claim": "Ambient observations never become ABI signals: the capture stream is written to a side SQLite store and never passes through scrutiny or tiered memory, despite ambient-capture.md:96-129 stating each pushed batch becomes an ABI signal absorbed into tiered memory.",
  "severity": "critical",
  "evidence": "POST /observations handler at /Users/shooby/Dev/openAGI/src/hosted-interface.js:408-415 only calls runtime.observations.record() (SQLite FTS). processSignal is invoked solely from chat turns (/Users/shooby/Dev/openAGI/src/agent-host.js:77) and the daily-adaptation-review cron (/Users/shooby/Dev/openAGI/src/abi-runtime.js:607); the generic signal-ingest path processIntegrationEvent (hosted-interface.js:297) is never fed by capture. iMessage bridge likewise writes straight to /memory/remember bypassing scrutiny (/Users/shooby/Dev/openAGI/src/integrations/imessage-bridge.js:208). Live ~/.openagi/observations is 742MB \u2014 the primary sensor \u2014 and none of it flows through the Signals\u2192Scrutiny\u2192Memory loop the whitepaper defines.",
  "verdict": "confirmed",
  "corrected": "Ambient observations never become ABI signals and never enter tiered memory, contradicting docs/scope/ambient-capture.md Phase 1.1 (each observation \"becomes an ABI signal of taskType: 'ambient-capture'\" that \"memory absorbs via the existing tier system\"). POST /observations (src/hosted-interface.js:408-417) only calls runtime.observations.record() into a side SQLite FTS5 store (src/observation-store.js, index.db); the string \"ambient-capture\" as a taskType appears nowhere in src/ or test/. processSignal is invoked only from chat turns (src/agent-host.js:77) and the daily-adaptation-review cron (src/abi-runtime.js:607); the generic ingest path processIntegrationEvent (abi-runtime.js:475, exposed at POST /ingest, hosted-interface.js:295-299) is never fed by capture \u2014 the Mac app's CaptureBridge.swift posts only to /observations. The iMessage bridge (src/integrations/imessage-bridge.js:208) posts to /memory/remember, which does land in tiered memory (hosted-interface.js:243) but bypasses processSignal/scrutiny. The live store (~/.openagi/observations/index.db, 742MB, ~22k activity + ~133k frame rows, written today) is the highest-volume sensor and none of it flows through the Signals\u2192Scrutiny\u2192Memory loop. Minor caveat: observations are not fully inert \u2014 they are read-side inputs to chat prompt grounding (agent-host.js:129), pattern-miner skill suggestions (pattern-miner.js:47), and proactive-observer evidence (proactive-observer.js:88,375) \u2014 but none of those paths create signals, invoke scrutiny, or write memory, so the spec's promised condenser distillation of ambient patterns into principles also never occurs.",
  "notes": "Every specific evidence pointer in the claim checked out: file paths, line numbers (handler is 408-417 vs claimed 408-415 \u2014 trivial), call-site inventory, and live-store size (du reports 742M; the DB file is ~770MB allocated). Verified read-only; queried the live DB with mode=ro for counts/recency only. Attempts to refute via indirect wiring (pattern-miner, proactive-observer, daily-recap, memory-condenser, boot/index wiring, test suite) found only read-side consumers \u2014 no path from ObservationStore to processSignal, scrutiny, or memory.remember. Test coverage (test/abi-runtime.test.js:1156+) exercises recall/search of observations, not absorption."
 },
 {
  "id": "G2",
  "area": "propagation",
  "claim": "Propagation collapses to exactly two catch-all specialists because every chat signal is hardcoded to domain 'general' and one of two taskType constants, so bounded domain specialization (division into scoped specialists with own success metrics) never emerges from real work content.",
  "severity": "critical",
  "evidence": "messageToSignal at /Users/shooby/Dev/openAGI/src/agent-host.js:261-297 sets domain:'general' and taskType to either 'adaptation-review' or 'specialization-candidate'; propagation dedupes spawns by scope signature, capping the reachable specialist space at two. Live /Users/shooby/.openagi/agents/specialists.json after ~2 months of operation contains exactly 2 specialists: 'general-adaptation-review-specialist' (52 activations) and 'general-specialization-candidate-specialist' (533 activations) \u2014 generic buckets, not the bounded-scope specialists of abi-architecture.md:39-49 / WHITEPAPER 'division, not multiplication'.",
  "verdict": "confirmed",
  "corrected": "Propagation in practice collapses to exactly two catch-all specialists: every internally-produced signal hardcodes domain 'general' \u2014 messageToSignal (src/agent-host.js:261-297, which serves chat plus all channels, scheduled prompts, and autopilot) picks between only two taskType constants ('specialization-candidate' vs 'adaptation-review') via a keyword regex, and the daily cron review job (src/cron-scheduler.js:93-115) hardcodes the same 'general'/'adaptation-review' pair. The propagation dedupe signature is hash(workflowId, domain, taskType, goal) (src/propagation-controller.js:177-184), and both taskTypes map to the two fixed default workflows (src/workflow-registry.js:46-66), so all internal signals collapse to exactly 2 signatures. Signal fields for content-derived scoping (specialistScope, goal, successMetric) exist but are never set by any producer. The live store (~/.openagi/agents/specialists.json) after ~1 month of operation (created 2026-06-06, not ~2 months) contains exactly 2 specialists \u2014 general-adaptation-review-specialist (52 activations) and general-specialization-candidate-specialist (533 activations) \u2014 whose boundedScope/successMetric are verbatim copies of default workflow text, not the bounded, work-derived scopes promised by abi-architecture.md's 'division, not multiplication'. Caveat that does not rescue the spec: the bounded-division machinery itself is real and tested (scoped tool allowlists enforced at invoke time, maxSpecialists=25, depth/breadth caps, quality-based retirement \u2014 test/specialist-bounds.test.js), and POST /ingest via the default 'abi' integration (src/integration-registry.js:30-97) would accept arbitrary domain/taskType and mint differently-scoped specialists \u2014 but no shipped code path derives domain or scope from actual work content, so specialization only emerges if an external caller hand-labels signals, which has never happened in live operation.",
  "notes": "Verified every path into PropagationController.propagate: abi-runtime.processSignal is the sole entry, fed by (1) agent-host.messageToSignal (all chat/channel/scheduled/autopilot traffic), (2) the daily-adaptation-review cron job, (3) processIntegrationEvent via POST /ingest (hosted-interface.js:295-299). Paths 1-2 hardcode general + two taskTypes; path 3 permits arbitrary domains but has no internal producer (BuildBetter webhook only triggers a task sync). Live specialists.json inspected for counts/names/dates only: exactly 2 specialists, matching the claimed names and activation counts (52 / 533). Two evidence-level inaccuracies corrected: operation span is ~1 month (created 2026-06-06), and the dedupe key is a workflow/domain/taskType/goal hash rather than a 'scope signature' \u2014 functionally equivalent to the claim's conclusion. The claim's core assertion \u2014 bounded domain specialization never emerges from real work content \u2014 is genuinely borne out by both code and live data."
 },
 {
  "id": "G3",
  "area": "skills / proactivity",
  "claim": "The mine\u2192notify\u2192accept\u2192replay loop has never closed: miner-produced skill candidates have no delivery path to the user (they are not mapped into outreach/notifications), and zero skills have ever been materialized or replayed on the live install.",
  "severity": "critical",
  "evidence": "/Users/shooby/Dev/openAGI/src/outreach-mapper.js maps only draft-created / proactive-suggestion / pending-action / clarification-created; pattern-miner and session-miner results are emitted only as 'miner-result' (/Users/shooby/Dev/openAGI/src/abi-runtime.js:659-669), an event no notifier consumes \u2014 candidates are dashboard-only. Live: /Users/shooby/.openagi/skills-suggested holds 74 candidates (newest 2026-06-28, rotting), while /Users/shooby/.openagi/skills and /Users/shooby/.openagi/replay are both empty. README's 'skill-draft notifications', 'one-click accept', and 'boring half of your work by month two/three' (README.md:32,64,126-131) are unrealized end-to-end.",
  "verdict": "partial",
  "corrected": "The mine\u2192notify\u2192accept\u2192replay loop is fully implemented in code but has never closed in practice, and its notify leg is transient rather than durable. Notify: miner candidates are NOT dashboard-only \u2014 each new candidate fires a per-candidate 'skill-candidate' event (src/pattern-miner.js:101, src/session-miner.js:97; the reviewer's cited 'miner-result' at abi-runtime.js:659-669 is a separate summary event) that hosted-interface.js:56 broadcasts over SSE and the Mac app turns into a native notification (mac/Sources/OpenAGI/AppState.swift:266-276, 'OpenAGI learned a new skill', deep-linked to the Skills tab), matching README:129. Accept: one-click accept is wired end-to-end \u2014 suggestion-feed.js merges miner candidates into GET /proactive/suggestions, and POST /proactive/suggestions/:id/accept (hosted-interface.js:1168-1188) materializes them into real SKILL.md files via createSkillFromCandidate (src/skill-materialize.js:32), with optional cron scheduling; replay (src/skill-replay.js) is constructed and SSE-bound (abi-runtime.js:215, hosted-interface.js:71). Both legs have unit tests (test/abi-runtime.test.js:1198-1228, 1704-1760). The real gaps: (1) 'skill-candidate' is absent from outreach-mapper.js's MAP, so candidates never become durable outreach items \u2014 excluded from the digest (outreach-config digestTypes = draft, suggestion), never re-pinged by outreach-stalled, and since miners run at 02:30/03:30 UTC the SSE notification is lost if no Mac/dashboard client is connected at that moment; (2) on the live install the loop has demonstrably never closed \u2014 /Users/shooby/.openagi/skills-suggested holds 74 candidates, all status 'pending' (newest 2026-06-29, ~6 days stale), with 0 accepted/rejected, /Users/shooby/.openagi/skills empty (0 materialized), and /Users/shooby/.openagi/replay empty (0 replay jobs).",
  "notes": "Claim's live-data half is confirmed exactly (74 pending / 0 materialized / 0 replayed); its code half is overstated and mislocated. The reviewer keyed on the 'miner-result' summary event and outreach-mapper's MAP, missing the parallel 'skill-candidate' event that reaches a real notifier (Mac native notification + browser Notification) and the complete accept\u2192materialize\u2192schedule and replay implementations with test coverage. The accurate defect is 'notification is transient/at-emission-time-only and skill candidates are excluded from the durable outreach store/digest/stalled re-ping', not 'no delivery path'. All 74 candidates sitting pending is consistent with either the transient-notification gap (no client connected overnight) or user non-engagement; code alone cannot distinguish."
 },
 {
  "id": "G4",
  "area": "scrutiny",
  "claim": "Scrutiny's seven axes receive hardcoded regex-keyword constants rather than measured per-signal values, so the verdict that 'drives everything downstream' is a function of a few keyword hits and the adaptive weight-fitting loop trains on near-constant inputs.",
  "severity": "major",
  "evidence": "messageToSignal at /Users/shooby/Dev/openAGI/src/agent-host.js:262-297: specificity always 0.65, confidence always 0.7, externalPressure 0.55, ambiguity 0.35; risk flips 0.35/0.75 on a keyword list (delete|deploy|payment|...), repetition 0.35/0.82 on schedule words, novelty 0.4/0.65 on 'remember'. The scorer itself (/Users/shooby/Dev/openAGI/src/directional-adaptive-scrutiny.js:29-88) supports all seven axes and the fitter is live (23 cycles in ~/.openagi/scrutiny/fitter-state.json), but the inputs carry almost no information about the actual signal \u2014 WHITEPAPER.md:296 / README.md:46 promise the axes assess signals, not keyword templates.",
  "verdict": "partial",
  "corrected": "The seven spec axes (WHITEPAPER.md/README.md: urgency/impact/novelty/repetition/risk/confidence/specificity) are never measured anywhere in the codebase: messageToSignal (src/agent-host.js:261-297) \u2014 the only path that records fitter-training outcomes \u2014 hardcodes specificity=0.65, confidence=0.7, externalPressure=0.55, ambiguity=0.35, conflict=0 and flips risk 0.35/0.75, repetition 0.35/0.82, novelty 0.4/0.65, impact 0.55/0.72 on regex/flag checks; cron-scheduler, hosted-interface, and integration-registry defaults are likewise constants, and this has been true since the initial commit. Live data confirms the axes carry almost no information: across all 524 current-pipeline outcomes, risk is 0.35 in every row (the keyword list never fired) and the derived evidence dimension is a single constant 0.697 \u2014 so its Pearson correlation is exactly 0 and the fitter can never learn its weight \u2014 while environment/company take only 2 values each. HOWEVER, the verdict is not purely keyword hits: one genuinely measured input exists \u2014 memoryReadiness computed from real memory-retrieval scores \u2014 which drives the memory and uncertainty dimensions (83/55 distinct values in live data) and also gates action selection (memories.length). And the fitter (live, 23 cycles, auto-apply) trains on the five derived dimensions, not the seven axes; its current sample is dominated by 1394 June 7-16 rows from an older code version with wide continuous variance, so 'trains on near-constant inputs' is accurate for rows today's pipeline produces (3 of 5 dims constant/near-constant) but not for the majority of its present training set.",
  "notes": "Every code citation in the claim verified exactly (agent-host.js:261-297, directional-adaptive-scrutiny.js:23-102, fitter-state.json cycles=23). agent-host.js:180 is the sole writer of scrutinyDimensions to the outcome store; ScrutinyJudge only nudges weights, never measures signals; no test covers messageToSignal axis values. Downgraded to partial for two overstatements: (1) memory retrieval is a real measured input to the verdict and fitter (memory/uncertainty dims genuinely vary: stdev 0.11/0.03, 83/55 distinct values in 524 live rows), so 'a function of a few keyword hits' omits the one live signal; (2) the fitter fits 5 derived dimensions (environment/company/evidence/memory/uncertainty), not the 7 axes, and 1394 of its ~1918 qualifying training rows (June 7-16, older dims format lacking risk/novelty/repetition keys) show wide 0-1 variance, so present-tense 'trains on near-constant inputs' is only true of newly produced rows. Strengthening findings the reviewer missed: risk=0.35 in 100% of live outcomes (keyword never fired), and evidence dim is exactly constant (0.697), making its correlation delta permanently zero."
 },
 {
  "id": "G5",
  "area": "outcome feedback (Foundation)",
  "claim": "Outcome 'quality' predominantly measures activity rather than success: the resolution sweep assigns a constant 0.7 to any run that executed at least one tool call, and the user-followup tone path has never resolved a single live outcome, so all downstream learners (fitter, retirement, routing quality) train on 'did it do anything' instead of 'did this work?' (abi-completion.md:16-30).",
  "severity": "major",
  "evidence": "resolveSweep at /Users/shooby/Dev/openAGI/src/outcome-store.js:168-183: score=0.7 if toolCalls.length>0, 0.5 if quiet, 0.4 if no reply in 6h. Live /Users/shooby/.openagi/outcomes/snapshot.json: 1035 of 2000 resolved outcomes are exactly 0.70; resolution sources are {system-inferred:1894, task-completed:82, timeout:24} with zero 'user-followup' tone resolutions. Fixed constants also at /Users/shooby/Dev/openAGI/src/skills.js:109 (0.7 on any skill completion) and skill-replay.js:115. The spec itself flags outcome-quality inference as the load-bearing unproven assumption (abi-completion.md:283).",
  "verdict": "confirmed",
  "corrected": "Outcome 'quality' in live operation predominantly measures execution activity, not success: resolveSweep (src/outcome-store.js:167-183) scores any cron/autopilot fire with >=1 tool call a constant 0.7 \u2014 ignoring the per-call ok/failed flags agent-host.js records, so a run of all-failed tool calls still scores 0.7 \u2014 with 0.5 for quiet fires and 0.4 for unanswered sent-messages. The user-followup tone path is fully wired (agent-host.js:208 stamps metadata.outcomeId; inferToneScore has test coverage) but has never fired on live data: the live 2000-outcome snapshot has 0 user-followup and 0 explicit-rating resolutions (sources: system-inferred 1894, task-completed 82, timeout 24; 1023 resolved at exactly 0.70), and every resolution in the last three weeks is a 0.7 autopilot-fire, a 0.7 auto-completed task (task-store.js:205), or a null timeout. Downstream learners \u2014 scrutiny-fitter correlation fits, propagation-controller meanOutcomeQuality/retirement (floor 0.3) and specialist-router qualityAdjust ((mean-0.5)*0.3) \u2014 therefore train on 'did it execute tools' rather than 'did this work'. Two nuances vs the original claim: skill-replay.js:115 is not a fixed constant (0.0 on error / 0.5 dry-run / 0.9 success) and skills.js:112 scores 0.1 on thrown failure, so hard execution failures are penalized \u2014 usefulness is what goes unmeasured; and the spec explicitly sanctions 'start heuristic, layer LLM later' (abi-completion.md:29), so this is the spec's own flagged load-bearing risk (line 283) materializing rather than an undisclosed deviation.",
  "notes": "Verified read-only. Key files: /Users/shooby/Dev/openAGI/src/outcome-store.js (resolveSweep, inferToneScore), src/agent-host.js:172-217 (outcome recording, outcomeId stamping, per-call ok flags), src/hosted-interface.js:1410 (sweep on ticker), src/skills.js:107-112, src/skill-replay.js:115, src/task-store.js:204-206, src/scrutiny-fitter.js, src/propagation-controller.js:129-170, src/specialist-router.js:105-125, docs/scope/abi-completion.md:15-30,283. Live store /Users/shooby/.openagi/outcomes/snapshot.json: counts only inspected, no content quoted. Claim's 1035@0.70 is now 1023 (rolling 2000-item snapshot). One anomaly not needed for the verdict: 1394 agent-reply resolutions dated Jun 7-16 carry source 'system-inferred' with score patterns (evidence+noise) matching test/abi-runtime.test.js seeding code rather than any current src path \u2014 likely demo/seed or removed code; all post-Jun-17 data matches current code exactly."
 },
 {
  "id": "G6",
  "area": "main-and-nodes",
  "claim": "Capture is hardwired to localhost while this machine is paired to a remote Distiller main, producing exactly the two-brain 'cancerous multiplication' the docs warn against: the main that holds keys/integrations never sees the ambient stream, and the Mac's full local daemon accumulates a divergent memory.",
  "severity": "major",
  "evidence": "/Users/shooby/Dev/openAGI/mac/Sources/OpenAGI/Capture/CaptureBridge.swift:31-38 \u2014 TODO(roadmap/remote-capture) with hardcoded URL http://127.0.0.1:43210/observations (no configurable daemon URL, no sourceMachineId, no offline queue per ROADMAP.md:7-34). Live /Users/shooby/.openagi/node.json pairs this Mac to 100.73.29.88:43210 (Distiller main), yet the packaged app runs its own full daemon writing 742MB of observations plus separate memory/vectors/outcomes locally. MAIN-AND-NODES.md:79-83 admits capture-node mode is not done; on the deployment actually in use this splits the brain from the eyes.",
  "verdict": "confirmed",
  "corrected": "Capture is hardwired to localhost \u2014 mac/Sources/OpenAGI/Capture/CaptureBridge.swift:38 POSTs to http://127.0.0.1:43210/observations under a TODO(roadmap/remote-capture) at lines 33-37, and AppState.swift:14 hardcodes the same base URL \u2014 while this machine's live ~/.openagi/node.json pairs it to a remote Distiller main at 100.73.29.88:43210. The pairing is honored only by the CLI (src/cli-client.js); nothing in the daemon or Mac app reads node.json, and no forwarding/replication path for observations exists anywhere in src/. There is no sourceMachineId in src/, mac/, or test/. The result on this deployment is the two-brain split the docs themselves label cancerous multiplication (WHITEPAPER.md:102, ROADMAP.md:33): the remote main never sees the ambient capture stream, while the Mac's packaged app runs a full local daemon actively accumulating a divergent brain (742MB observations index.db, memory, vectors, and outcomes all written within hours of verification). The split is made vivid by the fact that the same app already has remote-main plumbing for outreach (AppState.outreachRemoteURL \u2192 OutreachConsumer), so proactive decisions arrive from a brain that cannot see the screen. Two corrections to the original evidence: (1) \"no offline queue\" is inaccurate \u2014 CaptureBridge's local SQLite is a durable unpushed queue (rows marked pushed=1 only after daemon confirmation, retried every 30s); what's missing is backoff and connection-health UX, not offline persistence; (2) this is an openly documented roadmap gap (ROADMAP.md \"Status: Coming soon\", MAIN-AND-NODES.md:76-80), not a spec that promises working remote capture \u2014 the gap is between the deployed configuration and the architecture the docs advocate, not between code and a promised behavior.",
  "notes": "Verified adversarially with read-only access. Refutation attempts that failed: (a) searched for daemon-side observation forwarding to a paired main \u2014 none (grep forward/relay/replicat in src/observation-store.js, src/index.js); (b) searched for node.json or OPENAGI_REMOTE use outside the CLI \u2014 only src/cli-client.js; (c) searched for sourceMachineId/machineId schema support \u2014 absent from src, mac, and test; (d) checked whether the Mac app has any configurable daemon URL \u2014 only outreachRemoteURL (UserDefaults), which configures OutreachConsumer, not capture; (e) checked test/ \u2014 no remote-capture or capture-bridge coverage exists (the feature does not exist to test). Live-store facts (counts/recency only): observations/index.db 742MB mod Jul 5 10:18; memory files mod Jul 5 08:30; vectors/store.json mod Jul 5 01:50; node.json remote = http://100.73.29.88:43210. One evidence sub-claim refuted (offline queue exists via SQLite pushed-flag), and framing softened from \"spec vs implementation gap\" to \"deployed configuration contradicts the docs' own architecture warnings, with the gap explicitly acknowledged in ROADMAP.md and MAIN-AND-NODES.md\" \u2014 neither changes the core verdict."
 },
 {
  "id": "G7",
  "area": "scrutiny pulse (B3)",
  "claim": "The harsh-review pulse is not implemented as designed: abi-completion.md:116-127 specifies a Sunday-8pm act-threshold raise (0.68\u21920.85), but the shipped weekly-harsh-review is a free-text LLM autopilot prompt with no code path that modifies any scrutiny threshold.",
  "severity": "minor",
  "evidence": "HARSH_REVIEW_PROMPT at /Users/shooby/Dev/openAGI/src/abi-runtime.js:82-100 (KEEP/KILL/CHANGE/ASK bullets) registered as an 'autopilot' task at abi-runtime.js:246-256; no reference to scrutiny thresholds anywhere in the job, and DirectionalAdaptiveScrutiny thresholds have no cyclical writer. B3 is part of the doc's own 'minimum thesis-faithful' definition of done (abi-completion.md:254-277).",
  "verdict": "confirmed",
  "corrected": "B3 (abi-completion.md:116-127) is only two-thirds implemented: the weekly Sunday-8pm cadence and the skeptical review prompt exist (HARSH_REVIEW_PROMPT, src/abi-runtime.js:82-100, registered at :247-257 with nextSundayEvening()), but the spec's third mechanic \u2014 raising the scrutiny `act` threshold from 0.68 to 0.85 for that turn \u2014 has no code path. runAutopilot (abi-runtime.js:942+) passes only {agentId, prompt} into agentHost.handleMessage; DirectionalAdaptiveScrutiny.evaluate/selectAction (directional-adaptive-scrutiny.js:15,106) accepts no per-turn threshold override, and the sole call site (processSignal, abi-runtime.js:486) supplies none. No cyclical writer of scrutiny *thresholds* exists anywhere (scrutiny-fitter.js adjusts dimension weights only; scrutiny-panel styles have static thresholds; reconciliation-calibration.js tunes an unrelated reconciliation bound). The live cron store (.openagi/cron/jobs.json) confirms the deployed job carries only a prompt. Minor mitigation: the harsh-review turn is not entirely toothless \u2014 its KILL bullet directs real retire/cancel tool use and it still runs under normal 0.68 scrutiny \u2014 but the spec's defining \"elevated thresholds\" pulse is unimplemented.",
  "notes": "Refutation attempts all failed: no threshold-override parameter in handleMessage/processSignal/evaluate; no harsh-review-specific branch in agent-host.js (only an origin===\"autopilot\" activity-kind label at line 173); no test asserts a threshold raise (autopilot-gate.test.js:35-40 only checks the job fires without the queued-work gate); .openagi/scrutiny/ is empty and jobs.json's weekly-harsh-review input has keys [agentId, prompt] with lastRunAt null. Evidence citations in the claim (file, lines, job registration) all verified accurate."
 },
 {
  "id": "G8",
  "area": "mining adaptivity",
  "claim": "Mining thresholds never recalibrate from accept/reject ratios: ambient-capture.md:170-184 promises a weekly harsh review that retunes mining thresholds, but the pattern miner's confidence floor is a hardcoded constant with no writer and accept/reject feedback is consumed only by the proactive observer's prompt.",
  "severity": "minor",
  "evidence": "MIN_CONFIDENCE = 0.55 at /Users/shooby/Dev/openAGI/src/pattern-miner.js:24 (plus fixed judge-bypass constants at lines 76-81); grep shows suggestion-feedback is consumed only in /Users/shooby/Dev/openAGI/src/proactive-observer.js (prompt-level preference summary) and hosted-interface display \u2014 nothing feeds accept/reject ratios back into pattern-miner or session-miner thresholds. Live consequence: 74 candidates produced, 0 accepted, no throttling response.",
  "verdict": "confirmed",
  "corrected": "Mining thresholds never recalibrate from accept/reject feedback, despite ambient-capture.md:180 (section 3.4) promising the weekly harsh review would retune them. The pattern miner's confidence floor (MIN_CONFIDENCE = 0.55, src/pattern-miner.js:24) and judge-bypass gates (0.9 confidence / 5 occurrences, pattern-miner.js:81) are hardcoded with no runtime writer \u2014 the constructor accepts a minConfidence override but only tests pass one; session-miner's thresholds (session-miner.js:23-28) are likewise fixed. The weekly harsh review exists (cron job abi-runtime.js:246-259) but its prompt (abi-runtime.js:82-95) has no visibility into skill-candidate accept/reject counts and the agent has no tool that could adjust mining thresholds. SuggestionFeedback.categoryMultipliers() \u2014 whose own comment says it's for the pattern-miner \u2014 is consumed only by a read-only dashboard endpoint (hosted-interface.js:1215); preferenceSummary()/isMuted() feed only the proactive observer's prompt. The gap is even deeper than stated: SuggestionFeedback.computeStats() reads only proactiveObserver.list(), so resolutions of mined candidates could never influence feedback stats at all. Live store: 74 candidates in ~/.openagi/skills-suggested, all status \"pending\" (0 accepted and 0 rejected \u2014 so no accept/reject votes exist yet, but the wiring to react to them is absent regardless; the only throttle is the alreadyProposed() dedupe, which is not feedback-based).",
  "notes": "Verified read-only. Key files: src/pattern-miner.js (constants, judge bypass, alreadyProposed dedupe), src/abi-runtime.js:82-95 + 246-259 (harsh review prompt and cron job \u2014 no accept/reject visibility, no threshold tool in agent-host.js), src/suggestion-feedback.js (computeStats reads only proactiveObserver.list; categoryMultipliers has zero miner consumers), src/proactive-observer.js:139/173 (sole prompt-level consumers), src/hosted-interface.js:1210-1223 (display-only). Tests cover SuggestionFeedback in isolation only (test/abi-runtime.test.js:1900-1960). Live data checked as counts/statuses only, no content read. Minor corrections to the evidence: judge-bypass constant is at pattern-miner.js:81 (comment block 74-80), and the '0 accepted' live framing should note 0 rejected too \u2014 no resolution signal exists yet, though the structural absence of wiring holds independently."
 },
 {
  "id": "G9",
  "area": "introspection / vocabulary curation",
  "claim": "Introspector and VocabularyCurator are endpoint-only: no cron job ever invokes them, and vocabulary merge/deprecation proposals are never auto-applied, so the documented introspection and vocabulary-curation loops do not run unattended.",
  "severity": "minor",
  "evidence": "The full cron registration block at /Users/shooby/Dev/openAGI/src/abi-runtime.js:219-426 registers agent-pulse, harsh-review, retirement-sweep, self-update, scrutiny-fit, scrutiny-judge, pattern-mine, session-mine (x2), imessage-extract, observer/planner jobs \u2014 no vocabulary or introspect task. They are reachable only via GET /audit and /vocabulary + manual apply-merges endpoints in hosted-interface.js (~476-487); applyMerges requires a dashboard click. Both capabilities are in the documented full scope (abi-completion.md 'everything' tier).",
  "verdict": "partial",
  "corrected": "No cron job ever invokes the Introspector or VocabularyCurator: abi-runtime.js constructs both (lines 174-175) but never calls them, the default cron block (219-426) and the task dispatcher (lines 606-728) have no introspect/vocab task type, and the live cron stores contain no user-added job for them either. Vocabulary merges are propose-only until a human clicks apply (POST /vocabulary/apply-merges, hosted-interface.js:484-487); deprecations can never be applied at all \u2014 VocabularyCurator has proposeDeprecations but no apply method \u2014 so spec D4's promised \"auto-merge / auto-deprecate\" loop does not exist (live memory-events.jsonl shows zero vocab-merge events ever). Spec D5's \"weekly autopilot structural audit\" is likewise unscheduled. However, \"endpoint-only\" is overstated for the Introspector: audit() is also exposed as the get_audit agent tool (tool-registry.js:623-629), so a cron-driven autopilot/harsh-review turn could run an audit unattended at the agent's discretion \u2014 no prompt instructs it and no job guarantees it \u2014 and the D5 Health dashboard tab half is implemented (renderHealth fetching GET /audit). Net: the documented curation loop genuinely cannot run unattended; the introspection loop is unscheduled and discretionary rather than unreachable.",
  "notes": "Verified read-only. Key files: src/abi-runtime.js (174-175 construction; 219-426 cron registrations \u2014 claim's job list was slightly incomplete but directionally right, e.g. also daily-task-digest, daily-recap, weekly-retrospective, task-reminders, task-sweep, outreach-digest; 606-728 dispatcher has no vocab/introspect branch), src/vocabulary-curator.js (no applyDeprecations method exists), src/introspector.js (header comment falsely claims it drives the weekly autopilot review prompt \u2014 HARSH_REVIEW_PROMPT at abi-runtime.js:82-96 never references the audit), src/tool-registry.js:623-629 (get_audit tool \u2014 the one reachability path the claim missed), src/hosted-interface.js:476-487 + renderHealth/renderVocab (dashboard surfaces, manual apply button ~3575). Tests cover only the pure functions (test/abi-runtime.test.js:839-856), not scheduling. Live-store corroboration (counts only): 0 matches for vocab/introspect/audit in ~/.openagi/cron/jobs.json and repo .openagi/cron/jobs.json; 0 \"vocab-merge\" persist events in either memory-events.jsonl, so applyMerges has never run on this install even manually. Verdict is partial only because \"endpoint-only\" is technically wrong for the Introspector (agent tool + Health tab exist); the substantive gap \u2014 no scheduled introspection pass, no auto-applied vocabulary curation, contra abi-completion.md D4/D5 \u2014 is fully confirmed."
 },
 {
  "id": "G10",
  "area": "memory hygiene (intuition channel)",
  "claim": "Principle vectors are never garbage-collected when the underlying principles decay or are superseded, so stale principles keep surfacing through the C2 intuition channel injected into every chat turn.",
  "severity": "minor",
  "evidence": "Live /Users/shooby/.openagi/vectors/store.json holds 138 'principle'-namespace entries versus 72 principle-tagged items in /Users/shooby/.openagi/memory/memory-state.json (66+ orphans); no VectorStore.delete call exists in the memory decay/correction/reset paths of /Users/shooby/Dev/openAGI/src/memory-system.js or file-backed-memory-system.js, while /Users/shooby/Dev/openAGI/src/agent-host.js:118 searches that namespace to inject top principles as intuitions on every turn.",
  "verdict": "confirmed",
  "corrected": "Principle vectors are write-only: VectorStore.delete() (src/vector-store.js:42) has zero callers anywhere in src/ or test/, and no boot/migrate path reconciles the vector store with memory. When a principle is superseded via correct() (memory-system.js:127-178, hidden from memory retrieval by the supersededBy check at line 80), cap-evicted (enforceLimits, line 307), or lost to a memory-state reset, its vector entry survives forever. agent-host.js:116-118 then searches that namespace on every respond() call (chat, autopilot, and cron) and injects the top 3 hits as C2 intuitions, filtering only on cosine score \u2014 not supersededBy, not the condenser's quarantineUntil (so quarantined principles also surface immediately), and not whether the backing memory item exists. Live data bears it out worse than claimed: ~/.openagi has 138 principle vectors vs 72 principle memory items with 98 orphans (71%, not 66); the repo-local .openagi store has 280 vectors vs 4 principle items. One refinement: decay() itself never deletes principles because long-tier TTL is Infinity (memory-system.js:12), so 'decayed principles' is not an actual divergence path \u2014 supersession, quarantine bypass, eviction, and state resets are.",
  "notes": "Verified by direct code read and live-store ID cross-reference (counts only, no content quoted). Key anchors: src/vector-store.js:42 (dead delete method), src/memory-condenser.js:71 (only principle upsert; quarantineUntil stored in memory metadata but not enforced at search), src/memory-system.js:12 (long TTL Infinity), :80 (supersededBy hidden in memory retrieval only), :211/:307 (silent memory deletions), src/agent-host.js:118 (per-turn principle search, limit 3, minScore 0.1) and :299-334 (intuition block injection). Only vector test is upsert/persist/reload (test/abi-runtime.test.js:608); no GC/sync coverage. Event log (~/.openagi/memory/memory-events.jsonl) shows 1238 remember / 52 condense / 1 decay events and none of the 98 orphan IDs, consistent with a past memory-state wipe the vector store outlived."
 }
]