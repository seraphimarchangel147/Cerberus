[
 {
  "id": "context-compression",
  "claim": "Hermes preserves a byte-stable system prompt for prompt-cache hits and compresses long conversations via aux-LLM summarization with session splitting (agent/conversation_compression.py, parent_session_id chains); openAGI sends only the last 12 messages to the model (src/model-provider.js:88,237) \u2014 a hard sliding window with no summarization, no cache preservation, and silent loss of everything older.",
  "whyItMatters": "An always-on ambient agent accumulates long-running context (multi-day tasks, corrections, in-flight plans). A 12-message window forgets mid-task state and re-pays full token cost every turn; cache-stable prompts plus compression are what make 24/7 operation both coherent and affordable.",
  "verdict": "partial",
  "corrected": "Hermes-agent genuinely does everything claimed: the system prompt is built once per session and replayed verbatim so its bytes are stable across turns for upstream prompt-cache hits (conversation_loop.py ~836-845, with Anthropic cache_control breakpoints via apply_anthropic_cache_control and anthropic_prompt_cache_policy), and long conversations are compressed by summarizing middle turns with a cheap auxiliary LLM (context_compressor.py + auxiliary_client.py, invoked from conversation_loop.py), splitting sessions into parent_session_id-chained child sessions (conversation_compression.py ~779, ~893). openAGI does use a hard sliding window \u2014 both providers send only the last 12 messages (src/model-provider.js:88 and :237, messages.slice(-12)) with no summarization of the dropped turns, which simply vanish from model context (full history remains in the session store, and the separate memory system can resurface facts via memoryHits/recall, but conversation turns are never summarized). However, \"no cache preservation\" is overstated: openAGI's Anthropic provider does set cache_control {type: ephemeral} on its system prompt (model-provider.js:232) \u2014 it is just largely ineffective, because buildDefaultInstructions embeds per-turn memory hits and scrutiny state into that system text, so the cached prefix is not byte-stable and typically misses; the OpenAI path has no caching at all.",
  "notes": "Hermes half: fully confirmed by code reading (byte-stable prompt invariant + cache_control breakpoints in conversation_loop.py/agent_runtime_helpers.py; aux-LLM summarizer in context_compressor.py using auxiliary_client.call_llm; parent_session_id child-session creation in conversation_compression.py). openAGI half: slice(-12) sliding window and absence of conversation summarization confirmed at model-provider.js:88/:237; but the sub-claim \"no cache preservation\" is contradicted by the cache_control ephemeral marker at model-provider.js:232 (present though undermined by per-turn memory-hit injection into the system prompt), and \"silent loss of everything older\" applies to model context only \u2014 history persists on disk and the memory system can partially resurface it. Hence partial rather than confirmed."
 },
 {
  "id": "execution-surface",
  "claim": "Hermes ships ~45 core tools including terminal (local/Docker/SSH/Modal/Daytona/Singularity backends), file read/write/patch/search, 12 browser tools, web_search, and execute_code; openAGI's tool registry contains no shell, no file tools, no browser, and no web search (verified by grep of src/tool-registry.js) \u2014 action is limited to MCP servers, skill replay, and off-by-default computer-use.",
  "whyItMatters": "Proactivity is only valuable if the agent can complete what it proposes. openAGI can notice 'you do this every Tuesday' but often cannot execute the automation itself; hermes can research, browse, edit files, and run commands end-to-end, turning suggestions into finished work.",
  "verdict": "partial",
  "corrected": "Hermes ships ~49 core tools (toolsets.py _HERMES_CORE_TOOLS) including terminal with local/Docker/SSH/Modal/Daytona/Singularity backends (tools/environments/), file read/write/patch/search, exactly 12 browser tools, web_search, and execute_code \u2014 that half is fully accurate. openAGI's src/tool-registry.js does lack shell, file, and browser tools, and computer-use is off by default (OPENAGI_COMPUTER_USE=1, and even then only start/end session + screenshot). However, the \"no web search\" claim is wrong: openAGI always registers web_search and fetch_url tools via src/integrations/web-search.js (called unconditionally from abi-runtime.js:468); a grep of tool-registry.js alone misses tools registered from src/integrations/ (which also include env-gated Rize, calendar, and iMessage-search tools). Accurate framing: openAGI has no shell, no file I/O, and no browser automation \u2014 action is limited to MCP servers, skill replay, web_search/fetch_url, a few env-gated integrations, and off-by-default screenshot-only computer-use.",
  "notes": "Evidence: hermes-agent toolsets.py:31-80 (core tool list, 49 entries incl. 12 browser_* tools, terminal, read_file/write_file/patch/search_files, web_search, execute_code); tools/environments/{local,docker,ssh,modal,managed_modal,daytona,singularity}.py. openAGI: src/tool-registry.js (~35 tools, none shell/file/browser); src/integrations/web-search.js registers web_search + fetch_url, wired unconditionally in src/abi-runtime.js:466-468; computer-use gate at abi-runtime.js:428-435 and src/integrations/computer-use.js (3 tools only). The claim's tool count \"~45\" vs actual 49 is within rounding; the disqualifying error is \"no web search,\" which the reviewer's single-file grep methodology missed."
 },
 {
  "id": "channel-breadth",
  "claim": "Hermes's gateway serves ~28 messaging platforms (Telegram, Slack, WhatsApp, iMessage/BlueBubbles, Signal, Discord, email, SMS...) with DM pairing security and per-user/group/thread session isolation; openAGI has Telegram and Twilio channel classes in src/channels.js but the live install's channels store is empty \u2014 proactive outreach reaches the user only through Mac app notifications.",
  "whyItMatters": "An ambient personal agent must reach you when you're away from the desk \u2014 that's precisely when meeting-prep nudges, urgent-mail alerts, and digest delivery matter most. Mac-notification-only outreach dies the moment the laptop lid closes.",
  "verdict": "confirmed",
  "corrected": "Hermes's gateway supports roughly 27-28 messaging platforms \u2014 24 built-in Platform enum members in gateway/config.py (Telegram, Discord, WhatsApp + WhatsApp Cloud, Slack, Signal, Mattermost, Matrix, Home Assistant, email, SMS, DingTalk, Feishu, WeCom, WeChat, BlueBubbles/iMessage, QQ, Yuanbao, plus local/api/webhook transports) plus 20 bundled plugin adapters adding Google Chat, IRC, LINE, ntfy, Teams, SimpleX, and others. It has real DM pairing security (gateway/pairing.py: secrets-based one-time codes, expiry, rate limits, lockout, 0600 file perms, per OWASP/NIST SP 800-63-4) and deterministic per-user/per-group/per-thread session key isolation (gateway/session.py, group_sessions_per_user default on, WhatsApp JID canonicalization). openAGI's src/channels.js has only Telegram and Twilio SMS classes with no pairing/allowlist and only a passthrough sessionId; in the live install neither is configured (no TELEGRAM_*/TWILIO_* env keys) and the channels store is effectively dormant \u2014 telegram/ and sms/ subdirectories are empty and events.jsonl holds just 8 stale events (6 local, 2 sms, none Telegram) from early May. Proactive outreach is delivered only through the hosted interface's /outreach feed + SSE consumed by the Mac app's UNUserNotificationCenter notifications (mac/Sources/OpenAGI/Outreach/), never through messaging channels.",
  "notes": "Only nuance vs the original claim: \"channels store is empty\" is slightly overstated \u2014 .openagi/channels/events.jsonl contains 8 old events (6 channel:local, 2 channel:sms, zero telegram; last write May 6), though the telegram/ and sms/ message directories are literally empty and no channel credentials exist in .env. This does not change the substance: no live messaging channel is operational. Platform count method: unique union of the 24 built-in Platform enum values (5 of which are local/api/webhook-style transports rather than user messaging apps) and the 20 plugins/platforms adapters (12 overlap with built-ins) gives ~27 unique messaging platforms, matching \"~28\"."
 },
 {
  "id": "cron-hardening",
  "claim": "Hermes cron has a file-lock on the 60s tick, a 3-minute hard interrupt per job, missed-fire catchup windows (clamped 120s\u20132h), at-most-once CAS semantics via Chronos, and delivery into a separate session protecting role alternation; openAGI's cron-scheduler.js/file-backed-cron-scheduler.js has no lock, no catchup, no per-run timeout, and no overlap guard (verified by grep).",
  "whyItMatters": "The scheduler is the heartbeat of a proactive agent. Without catchup, a daemon restart silently skips the daily plan; without timeouts, one hung LLM call stalls every subsequent routine; without locking, double-fires send duplicate outreach and burn budget.",
  "verdict": "partial",
  "corrected": "Hermes cron (hermes-agent) has: a 60s tick with a file lock (~/.hermes/cron/.tick.lock, cron/scheduler.py:1-9,426) so only one tick runs at a time; a per-job timeout that is a 600s (10-minute) default INACTIVITY-based limit (HERMES_CRON_TIMEOUT, scheduler.py:2907-2993) ending in agent.interrupt() \u2014 NOT a 3-minute hard interrupt (the 180s constant in the repo is only a \"still working\" notify interval, gateway/run.py:18740); missed-fire catchup with a grace window of half the schedule period clamped 120s\u20132h (cron/jobs.py:488-491), firing once and fast-forwarding past the window; at-most-once semantics via preemptive next_run_at advancement plus a file-lock CAS claim (claim_job_for_fire, 300s TTL) used when the external Chronos provider fires across replicas; and delivery into the cron job's own separate session, with the optional transcript mirror appended as a user-role turn specifically to preserve strict role alternation. openAGI's cron-scheduler.js/file-backed-cron-scheduler.js indeed has no lock, no overlap guard (setInterval in hosted-interface.js:1407 fires runtime.tick() without awaiting; concurrent ticks can double-run a job since nextRunAt advances only after the handler resolves), and no per-job timeout (only a 120s per-API-call abort in model-provider.js). However, \"no catchup\" is overstated: overdue jobs do fire once on resume (missed runs collapse because nextRunAt is recomputed from now) and abi-runtime.js:584-603 emits a cron-catchup event for jobs >5min overdue \u2014 what openAGI lacks is Hermes's clamped 120s\u20132h grace-window policy and drift-free schedule tracking, not catchup entirely.",
  "notes": "Verdict rationale: each half is substantially correct but contains one false specific. Hermes half: lock, catchup clamp, CAS/at-most-once, and separate-session/alternation all CONFIRMED by code; \"3-minute hard interrupt\" REFUTED (it is a 10-min inactivity timeout, HERMES_CRON_TIMEOUT default 600s). openAGI half: no lock, no overlap guard, no per-run timeout all CONFIRMED; \"no catchup\" refuted in the strict sense (fire-once-on-resume behavior plus an explicit cron-catchup event exist), though the clamped-window policy is genuinely absent. Key files: hermes-agent/cron/scheduler.py, cron/jobs.py, cron/scheduler_provider.py, plugins/cron_providers/chronos/; openAGI/src/cron-scheduler.js, src/file-backed-cron-scheduler.js, src/abi-runtime.js (tick/runDue at 584-605), src/hosted-interface.js (ticker at 1406-1413). Read-only verification; no files modified."
 },
 {
  "id": "crash-recovery",
  "claim": "Hermes has an explicit crash-recovery state machine: resume_pending flags that resume the same transcript with a synthesized auto-continue MessageEvent, a .clean_shutdown marker gating recovery, and automatic suspension after 3+ restart loops; openAGI persists sessions via FileBackedAgentStore but has no resume machinery \u2014 an in-flight run interrupted by a daemon crash is simply abandoned (only an MCP-connect crash guard exists, commit c099bce).",
  "whyItMatters": "A 24/7 daemon will crash. If a proactive job or user request dies mid-flight and never resumes, the agent silently drops commitments \u2014 the single fastest way to lose a user's trust in an ambient assistant.",
  "verdict": "confirmed",
  "corrected": "Hermes has an explicit crash-recovery state machine: sessions interrupted mid-turn are marked resume_pending with a reason (restart_timeout / shutdown_timeout / restart_interrupted); on boot, absence of the .clean_shutdown marker triggers suspend_recently_active() to flag crashed in-flight sessions, and _schedule_resume_pending_sessions then synthesizes an empty auto-continue MessageEvent per fresh session that resumes the same transcript (a recovery system note is injected on that turn). Two threshold-3 loop breakers protect it: _suspend_stuck_loop_sessions auto-suspends any session active across 3+ consecutive restarts, and a separate restart_loop_guard circuit breaker (3 resume-boots within 60s) skips auto-resume for that boot without suspending. openAGI's FileBackedAgentStore (src/agent-store.js) persists agents.json and per-session transcripts but has no resume machinery \u2014 no resume flag, no clean-shutdown marker, no boot-time scan or continuation turn \u2014 so an in-flight run interrupted by a daemon crash leaves its partial transcript on disk and is simply abandoned; the only crash guard is commit c099bce's top-level unhandledRejection/uncaughtException handler in src/boot.js (added so MCP connect failures don't kill the daemon). openAGI does have unrelated crash-durability (pending-actions JSONL journal replay, task-store snapshots, cron missed-job catch-up), but none of it resumes interrupted agent runs.",
  "notes": "Evidence: hermes-agent gateway/run.py (lines ~5890 _STUCK_LOOP_THRESHOLD=3, ~6264 _AUTO_RESUME_REASONS, ~6355-6440 _schedule_resume_pending_sessions with restart_loop_guard check, ~6811 .clean_shutdown gate, ~8161 marker write), gateway/restart_loop_guard.py (DEFAULT_MAX_RESTARTS=3, DEFAULT_WINDOW_SECONDS=60), tests/gateway/test_restart_resume_pending.py and test_clean_shutdown_marker.py. openAGI: src/agent-store.js:84 FileBackedAgentStore, src/boot.js:57-64 process guards, git show c099bce. One precision fix: the claim's \"3+ restart loops \u2192 suspension\" maps to the stuck-loop suspender; the restart_loop_guard breaker (also threshold 3) skips auto-resume rather than suspending \u2014 the claim conflated two adjacent mechanisms but is substantively accurate."
 },
 {
  "id": "session-search",
  "claim": "Hermes gives the agent an on-demand session_search tool (SQLite FTS5 over all past transcripts plus LLM summarization) for mid-conversation recall of any prior exchange; openAGI's agent has recall (heuristic token-overlap over memory items) and recall_activity (FTS over screen OCR) but no tool to search its own chat history \u2014 transcripts are only touched by the batch session-miner.",
  "whyItMatters": "Memory distillation is lossy by design; the raw transcript is ground truth. 'What did we decide about X three weeks ago?' is a core personal-agent query that tiered memory alone answers with a compressed guess instead of the actual conversation.",
  "verdict": "partial",
  "corrected": "Hermes gives the agent an on-demand session_search tool for mid-conversation recall of any prior exchange: SQLite FTS5 over the full message store (all past sessions, even cross-profile), with four calling shapes (FTS discovery with snippets/bookends, anchored scroll, whole-session read, browse-recent) \u2014 but it is purely retrieval with NO LLM summarization; the code explicitly removed the old summary LLM path (\"No LLM calls anywhere \u2014 every shape returns actual messages from the DB\"; only a stale toolset description string still says \"with summarization\"). openAGI's agent has recall (heuristic tokenOverlapScore over stored memory items, not transcripts) and recall_activity (SQLite FTS5 over screen-OCR/window-title observations), plus a metadata-only list_sessions tool (message counts and last-message preview, no query), but no tool to search its own chat-history content. Full transcripts are read by several batch/infra paths \u2014 the session-miner (intent clustering for skill proposals), the proactive-observer (latest session), the outcome-store, and the hosted-interface HTTP API \u2014 not only the session-miner, though none of these is an agent-invocable search.",
  "notes": "Verdict is partial because the central asymmetry is confirmed (hermes has agent-facing FTS transcript search; openAGI does not), but each half contains one factual error. (1) Hermes: claim says \"plus LLM summarization\" \u2014 tools/session_search_tool.py docstring and schema state \"no summary LLM path\" / \"No LLM calls anywhere\"; the dual-mode summary path from PR #20238 was merged away. toolsets.py:222 has a stale description \"Search and recall past conversations with summarization\" that does not match the implementation. FTS5 confirmed in hermes_state.py (messages_fts virtual tables, BM25); registered as a tool in tools/session_search_tool.py:903. (2) openAGI: recall = tokenOverlapScore in src/memory-system.js:67-110 (confirmed heuristic over memory items); recall_activity = FTS5 texts table in src/observation-store.js (confirmed OCR/activity); no chat-content search tool in src/tool-registry.js (list_sessions at line 426 is metadata-only). But \"transcripts only touched by batch session-miner\" is overstated: src/proactive-observer.js:212, src/outcome-store.js:157, and src/hosted-interface.js:272 also read full sessions (src/daily-recap.js:265 reads session metadata)."
 },
 {
  "id": "skill-coldstart",
  "claim": "Hermes ships a bundled skill library (Apple apps, GitHub, Google Workspace, email, research, data-science, and dozens more), a /learn command that authors SKILL.md from anything describable, an agentskills.io hub, and a curator that tracks usage and auto-archives stale skills; openAGI acquires skills only through miner-suggested candidates \u2014 and after months of live operation has 74 suggested candidates, 0 accepted skills, and an empty skills directory.",
  "whyItMatters": "Mining-only acquisition means weeks of observation before the agent can do anything, and the live install proves the funnel converts at zero. A day-one library plus explicit teach-a-skill authoring delivers value immediately while mining catches up \u2014 and curation prevents the skill set from rotting.",
  "verdict": "partial",
  "corrected": "Hermes ships a bundled library of 72 SKILL.md skills (Apple apps, GitHub, Google Workspace under productivity, email, research, data-science, and more, plus an optional-skills tree), a /learn command whose prompt (agent/learn_prompt.py) has the agent author a SKILL.md from anything describable (dirs, URLs, the current chat, pasted notes), a Skills Hub (hermes_cli/skills_hub.py) whose skill format follows the agentskills.io open standard \u2014 though its code adapters install from GitHub repos and bundled optional-skills, with agentskills.io serving as the linked hub site \u2014 and a curator (agent/curator.py + tools/skill_usage.py) that tracks per-skill usage in a sidecar and auto-transitions unused skills active\u2192stale\u2192archived into .archive/. openAGI is far thinner but not skill-less and not miner-only: it bundles 12 example skills (examples/skills/) that the SkillRegistry loads by default (src/abi-runtime.js:438-443), and it can materialize skills from proactive-observer suggestions, from pattern-miner/session-miner candidates, or from manually dropped SKILL.md files; it has no /learn analog, no hub, and no usage-tracking/archival curator. Live state after roughly one month of operation (data dir created June 3, 2026): exactly 74 miner candidates in ~/.openagi/skills-suggested, all status \"pending\" (0 accepted), and an empty user skills directory (~/.openagi/skills) \u2014 though the 12 bundled example skills still load at runtime.",
  "notes": "Hermes half fully verified in code: 72 SKILL.md files under skills/ (find count); /learn at hermes_cli/commands.py:192 backed by agent/learn_prompt.py; Skills Hub in hermes_cli/skills_hub.py + tools/skills_hub.py (GitHub/optional-skills adapters, agentskills.io-compatible format, README links agentskills.io as \"Skills Hub\"); curator in agent/curator.py + tools/skill_usage.py with stale/archive lifecycle. openAGI half: candidate/accepted counts and empty user skills dir confirmed (74 JSONs all \"pending\" in ~/.openagi/skills-suggested; ~/.openagi/skills empty; only status fields inspected, no personal content read), but \"acquires skills only through miner-suggested candidates\" is refuted \u2014 12 bundled example skills load by default and observer-suggestion + manual paths exist \u2014 and \"months of live operation\" is ~1 month (data dir June 3, 2026; candidates June 6-28). Verdict partial because half 1 holds and half 2 holds only on the numbers, not the framing."
 },
 {
  "id": "turn-latency-learning",
  "claim": "Hermes persists learnings minutes after they occur: turn-count nudges trigger spawn_background_review, a forked agent whitelisted to memory/skill tools that decides what to persist right after each conversation; openAGI's learning runs are batch crons (session-mine hourly/nightly, pattern-mine 02:30) with no per-turn trigger, so a correction given at 9am isn't eligible for distillation until hours later.",
  "whyItMatters": "For 'corrections lock in, never repeat' \u2014 openAGI's own headline claim \u2014 latency is the mechanism. If the user corrects the agent and the same mistake recurs an hour later because distillation is nightly, the promise is broken; per-turn scoped review closes the loop inside the session.",
  "verdict": "partial",
  "corrected": "Hermes runs an in-session learning loop: turn-count nudges (memory: every N user turns, skills: every N tool iterations, both defaulting to 10 in agent_init.py) set review flags checked in turn_context.py/turn_finalizer.py, and after the response is delivered turn_finalizer.py calls _spawn_background_review, which forks the agent (agent/background_review.py), replays the conversation snapshot, and runs under a runtime tool whitelist limited to memory/skill toolsets, writing directly to the memory and skill stores \u2014 so learnings persist minutes after they occur, though at ~every-10th-turn cadence rather than after literally every conversation. openAGI's skill miners are indeed cron-batch with no per-turn trigger (abi-runtime.js: pattern-mine nightly 02:30, session-mine nightly 03:30 plus an hourly pass; memory condensation daily 03:30), but a 9am correction is NOT unpersisted for hours: the in-conversation correct_memory tool (tool-registry.js) locks the corrected fact into memory immediately (memory-system.js supersedes the stale item, exempts the correction from decay/eviction), and the hourly session-mine caps skill-mining latency at ~1 hour. What openAGI lacks is Hermes's forked post-turn reviewer that proactively decides what to distill; only its principle distillation (condenser) and OS-pattern mining truly wait until the nightly crons.",
  "notes": "Half 1 confirmed with a cadence nuance: hermes-agent/agent/turn_context.py:297-304 (memory nudge), turn_finalizer.py:454-478 (skill nudge + spawn after response), background_review.py:745-769 (fork + memory/skills tool whitelist, deny message \"Only memory/skill tools are allowed\"), defaults nudge_interval=10 (agent_init.py:1236,1332). Half 2 partly holds: cron schedules match exactly (openAGI/src/abi-runtime.js:293-314, condenser 03:30 at :223-227) and miners have no per-turn trigger, but the \"correction at 9am waits hours\" consequence is refuted by the correct_memory tool (src/tool-registry.js:268) which persists locked corrections instantly (src/memory-system.js:127-190), and hourly session-mine bounds mining latency at ~1h. No files modified; no personal .openagi content quoted."
 },
 {
  "id": "injection-hardening",
  "claim": "Hermes hardens against prompt injection structurally: YOLO mode frozen at import so injected text cannot self-escalate, a dangerous-command pattern engine with aux-LLM smart approval and persisted allowlists, and deliberately constrained toolsets for untrusted webhook payloads; openAGI ingests untrusted content (arbitrary screen OCR, incoming iMessages) into LLM prompts with approval expressed as tool-description text and a pending-actions queue that has never persisted an item live.",
  "whyItMatters": "An ambient agent's sensors are an injection firehose \u2014 any webpage or text message the user views becomes model input. Verdict-based tool policy helps, but capability escalation and side-effecting actions need structural guards, not prompt-level pleas, before openAGI's execution surface grows.",
  "verdict": "partial",
  "corrected": "Hermes hardens against prompt injection structurally, exactly as claimed: YOLO mode is frozen at module import (tools/approval.py:31-34, _YOLO_MODE_FROZEN, with an explicit comment that per-call env reads would be an injection escalation path); a DANGEROUS_PATTERNS regex engine plus a hardline blocklist that even yolo cannot bypass; _smart_approve() aux-LLM risk assessment with comment-stripping and delimiter-wrapping defenses; a permanent command_allowlist persisted to config.yaml; and a deliberately constrained webhook toolset (_HERMES_WEBHOOK_SAFE_TOOLS = web_search/web_extract/vision_analyze/clarify, toolsets.py:82-90) documented as protection against untrusted third-party payloads. openAGI does ingest untrusted content into LLM prompts \u2014 screen OCR snippets (proactive-observer.js:227) and incoming iMessages forwarded to the agent, open to all senders when no allowlist is configured (imessage-bridge.js) \u2014 and its pending-actions queue has never persisted an item live (both ~/.openagi/pending-actions and the repo's .openagi/pending-actions are empty, no journal or snapshot ever written). However, openAGI's approval is NOT merely tool-description text: tool-registry.js:157 is a code-enforced gate that intercepts tools flagged needsConfirmation before the handler runs and diverts them into a file-backed PendingActionStore, and agent-host.js:87-100 structurally enforces scrutiny verdicts (read-only tool filtering, confirm-gating all sideEffects tools, or stripping tools entirely). The accurate criticism is coverage, not mechanism: only ~5 tools carry needsConfirmation, replay_skill (AppleScript/keyboard control of the Mac) has neither needsConfirmation nor sideEffects so it escapes every gate, draft-only/\"never send it yourself\" semantics live purely in description text, and openAGI has nothing comparable to Hermes's dangerous-command pattern engine, aux-LLM guard, or provenance-constrained toolsets for its untrusted OCR/iMessage inputs.",
  "notes": "Hermes half: fully confirmed with code evidence (approval.py freeze comment, _smart_approve calling agent.auxiliary_client with anti-injection defenses, save_permanent_allowlist to config.yaml, _HERMES_WEBHOOK_SAFE_TOOLS). openAGI half: 3 of 4 elements confirmed (OCR-to-prompt, iMessage-to-prompt, empty live pending-actions dirs verified by metadata only \u2014 no personal content read), but \"approval expressed as tool-description text\" is refuted: a structural needsConfirmation interception plus scrutiny-verdict tool policy exists in code, though its coverage is thin (unflagged side-effecting tools like replay_skill bypass it). Since one half holds fully and the other only partially, verdict=partial per the given rubric."
 },
 {
  "id": "parallel-delegation",
  "claim": "Hermes has on-demand parallel task decomposition: delegate_task spawns isolated-context subagents (sync or background, concurrency-capped, leaf/orchestrator role separation, spawn depth 2) plus a durable SQLite kanban dispatcher; openAGI's propagation only creates persistent threshold-triggered specialists for routing \u2014 there is no mechanism to fan one large task into parallel workers.",
  "whyItMatters": "Proactive work items are often big (research a vendor, triage a backlog, prep a meeting across five sources). Specialists-as-routing-targets don't shorten a single job; parallel subagents with role guardrails do, and background durability lets proactive jobs run without blocking the chat.",
  "verdict": "confirmed",
  "corrected": "Hermes has on-demand parallel task decomposition: delegate_task (tools/delegate_tool.py) spawns isolated-context subagents (fresh conversation, skip_context_files/skip_memory, blocked-tool stripping), in sync batches or background via async_delegation.py's daemon executor, capped by delegation.max_concurrent_children (default 3, governs both modes), with leaf/orchestrator role separation; spawn depth defaults to 1 (flat parent\u2192children) via MAX_DEPTH, configurable through delegation.max_spawn_depth (floor 1, no ceiling; nested orchestration requires \u22652) \u2014 not a fixed depth of 2. It additionally has a durable SQLite kanban dispatcher (hermes_cli/kanban_db.py) with worker subprocesses, dispatch locks, task decomposition (kanban_decompose.py), and a swarm topology of parallel workers + verifier + synthesizer (kanban_swarm.py). openAGI's propagation (src/propagation-controller.js) only creates persistent, threshold-triggered bounded specialists (repetition \u22650.72, risk\u00d7novelty \u22650.62, memory under-coverage, or explicit request; max 25, depth 3, breadth 5 \u2014 so specialists can sub-propagate, but still only as persistent agents) that a router (src/specialist-router.js) matches one-at-a-time against incoming signals; its task store is a sequential JSONL to-do queue drained by a single 30-minute autopilot pulse, and there is no mechanism to fan one large task into parallel workers.",
  "notes": "Both halves verified by reading code. Only inaccuracy in the original claim: \"spawn depth 2\" \u2014 the actual default is MAX_DEPTH=1 (parent\u2192child, flat), with delegation.max_spawn_depth configurable (floor 1, no ceiling); orchestrator nesting activates when it is raised to \u22652. All other Hermes specifics confirmed: DELEGATE_BLOCKED_TOOLS blocks recursive delegation for leaves, orchestrator role re-adds the delegation toolset, _get_max_concurrent_children (default 3) caps sync and background alike, async_delegation.py provides background handles via the shared completion queue, and the kanban subsystem is SQLite-backed with dispatcher-spawned worker subprocesses. openAGI verified to lack fan-out: propagation is threshold-gated specialist creation for routing; agent-host routes one specialist per signal; autopilot drains the agent task queue sequentially. Minor fairness note: openAGI specialists can sub-propagate to depth 3, so 'routing only' understates it slightly, but the substance (no parallel worker fan-out) holds."
 },
 {
  "id": "blueprint-ux",
  "claim": "Hermes wraps automations in blueprints \u2014 parameterized templates with typed slots rendered as dashboard forms and pre-filled slash commands ('users never type raw cron'), sourced from a curated catalog, skills, usage patterns, and new integrations, with dismissals that latch; openAGI's suggestions are freeform LLM-proposed text with accept/reject, no typed parameters or template catalog.",
  "whyItMatters": "The gap between 'agent noticed a routine' and 'user schedules it' is where proactive value is won or lost. Typed slots make accepted suggestions immediately correct (right time, right channel, right filter) instead of requiring a follow-up conversation to fix a freeform guess.",
  "verdict": "confirmed",
  "corrected": "Hermes has a full blueprint system: cron/blueprint_catalog.py defines parameterized automation blueprints with typed slots (time/enum/text/weekdays, defaults, options, validation) sourced from a curated in-repo CATALOG of ~14 entries; blueprint_form_schema() drives dashboard forms (web/src/components/AutomationBlueprints.tsx, one field per slot) and blueprint_slash_command() emits pre-filled /blueprint commands \u2014 the docstring states verbatim 'users never type raw cron' (with a free-text schedule slot as an explicit escape hatch, and a second skill-carried blueprint variant in tools/blueprints.py that does embed a literal cron string in SKILL.md frontmatter). Automation suggestions flow through one surface (cron/suggestions.py) with sources exactly {catalog, blueprint (skill-carried), usage, integration}, and dismissals latch permanently by a stable dedup_key. openAGI, by contrast, has LLM-proposed suggestions (src/proactive-observer.js) whose automation content is freeform text (title/rationale/steps/draftBody in a structured JSON envelope), resolved only via accept/reject/dismiss endpoints (src/hosted-interface.js, src/suggestion-feed.js) \u2014 no typed parameters, no automation template catalog, no forms or pre-filled commands. Minor softeners: openAGI's MCP-connection suggestions are deterministically catalog-triggered (mcp-catalog.js) rather than LLM-proposed, and it has time-windowed dedup by title/mcpId plus per-category muting \u2014 weaker than Hermes's permanent latch, but some repeat-suppression exists.",
  "notes": "Key evidence: hermes-agent cron/blueprint_catalog.py (BlueprintSlot, CATALOG, blueprint_form_schema, blueprint_slash_command, 'users never type raw cron' docstring, fill_blueprint validation), cron/suggestions.py (VALID_SOURCES frozenset, dismissed-latch-by-dedup_key), tools/blueprints.py (skill-carried blueprints registering suggestions on install), web/src/components/AutomationBlueprints.tsx (form rendering), hermes_cli/blueprint_cmd.py (/blueprint command). openAGI: src/proactive-observer.js (LLM JSON schema with freeform steps for category=automation; alreadyProposedRecently time-window dedup), src/suggestion-feed.js (accept/reject/dismiss envelope), src/hosted-interface.js (resolution endpoints, category mute via suggestionFeedback). The claim's only imprecisions are that Hermes 'blueprint' names two mechanisms (typed-slot catalog vs raw-cron skill frontmatter) and that openAGI suggestions are structured JSON envelopes with some dedup/muting rather than pure raw text \u2014 neither undermines the core contrast, so verdict=confirmed."
 },
 {
  "id": "surface-ubiquity",
  "claim": "One hermes agent instance is reachable from CLI, a React/Ink TUI, a web dashboard, a native desktop app, editors via ACP, voice (push-to-talk, TTS, a Google Meet bot), and every messaging platform \u2014 with cross-platform conversation continuity; openAGI's interaction surfaces are the Mac overlay/notifications and a local hosted interface, and audio is explicitly out of scope.",
  "whyItMatters": "A personal agent's usefulness scales with how often you can talk to it. Continuity across surfaces means the thread you start in the terminal continues on your phone; hermes's Meet bot also captures the largest ambient signal openAGI currently forfeits \u2014 live meetings \u2014 without violating openAGI's no-mic stance on the daemon itself.",
  "verdict": "partial",
  "corrected": "Hermes-agent does expose one agent instance across a CLI (cli.py/hermes_cli), a React/Ink TUI (ui-tui, ink 6.8 + React 19), a web dashboard (web/, Vite+React config/session monitor), an Electron (not strictly native) desktop app (apps/desktop), editors via an Agent Client Protocol adapter (acp_adapter/), voice \u2014 push-to-talk and continuous VAD recording plus TTS (hermes_cli/voice.py, tools/voice_mode, tools/tts_tool) and a Google Meet bot plugin (plugins/google_meet/meet_bot.py) \u2014 and roughly 25 messaging platforms (Telegram, Discord, Slack, WhatsApp, Signal, iMessage/BlueBubbles, Teams, Matrix, Mattermost, LINE, Google Chat, DingTalk, Feishu, WeCom, WeChat, QQ, IRC, email, SMS, ntfy, SimpleX, and more) \u2014 \"every messaging platform\" is hyperbole. Cross-platform conversation continuity is advertised in the README, but sessions are keyed per-platform (gateway/session.py build_session_key); continuity comes from all surfaces attaching to one gateway plus pooled memory, not literally merged cross-platform sessions. OpenAGI's audio exclusion is accurate (docs/scope/ambient-capture.md: \"Audio/microphone capture. Out of scope.\"), but its interaction surfaces are broader than just the Mac overlay/notifications and local hosted interface: it also ships an openagi CLI client (src/cli-client.js, local or remote-main \"node\" mode), a Telegram channel and a Twilio SMS channel (src/channels.js), and a two-way iMessage bridge that turns a Mac into a conversational gateway (src/integrations/imessage-bridge.js). The direction of the comparison stands \u2014 hermes covers far more surfaces (TUI, web UI, desktop app, editors, voice/meetings, ~25 chat platforms) \u2014 but openAGI is not overlay-plus-web-only.",
  "notes": "Half 1 verified in code: ui-tui/package.json (ink+react), apps/desktop/package.json (Electron), acp_adapter/server.py (ACP), hermes_cli/voice.py (push-to-talk + TTS), plugins/google_meet/meet_bot.py, gateway/platforms/ + plugins/platforms/ (~25 adapters). Caveats: desktop is Electron; \"every messaging platform\" is hyperbole; continuity is a README claim while build_session_key isolates sessions per platform. Half 2 fails on the surface list: openAGI also has a CLI client, Telegram, Twilio SMS, and a two-way iMessage bridge; audio out-of-scope confirmed verbatim in docs/scope/ambient-capture.md:28. No personal content from .openagi was read or quoted."
 }
]